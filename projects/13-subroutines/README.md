# Project 13: The Subroutine Library

**Concepts**: `call`/`ret`, `proc`/`endp`, parameter passing via registers.

**Task**: Refactor Projects 5-10 into reusable subroutines (PrintString,
ReadChar, etc.).

**Reward**: Code organization. No more spaghetti.

