# Project 11: Hex Dump Viewer

**Concepts**: Hexadecimal, `shr`/`shl`, masking (`and`), nibble extraction.

**Task**: Input a byte value, print it as 2-digit hex.

**Reward**: You can now read memory dumps.

