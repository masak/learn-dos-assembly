# Project 27: Serial Port Chat (Loopback)

**Concepts**: UART ports (`0x3F8`), `in`/`out` instructions, baud rate,
loopback test.

**Task**: Send a byte through COM1 and read it back. Display result.

**Reward**: Serial communication fundamentals.

