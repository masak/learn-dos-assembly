# Project 38: DOS Shell Extension

**Concepts**: `INT 0x21`, `AH=0x4B` (EXEC), command parsing, environment
strings.

**Task**: A simple command shell that can run .COM files and has `DIR`, `TYPE`,
`CD`.

**Reward**: You're writing an operating system component.

