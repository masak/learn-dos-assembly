# Project 28: The Timer Interrupt

**Concepts**: `INT 0x08`/`0x1C` hooking, interrupt chaining, `iret`, `pushf`/
`call far`.

**Task**: Hook `INT 0x1C` to increment a counter. Display "TICKS: X" in a
corner.

**Reward**: Background processing. Multitasking concepts.

